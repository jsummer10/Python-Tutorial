# Python Control Flow

Control flow is how a program makes decisions and repeats work. This lesson
covers Python's branching and looping tools, including modern pattern matching.

## Lessons

1. [Control Flow](01_control_flow.ipynb)

## Topics

- `if`, `elif`, and `else`
- `match` and `case`
- `for` and `while` loops
- `break`, `continue`, and loop `else` blocks
- Iterating over common data structures
